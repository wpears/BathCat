to cdn copy:
baseUrl: (location.protocol === 'file:' ? 'http:' : location.protocol) + "../api/js/dojo/dojo"

commented out event listeners in dojo touch...! need to build this out

to esri/MouseEvents: return from _onMouseDragHandler

structure of _onPanHandler in TiledMapServiceLayer


resetTrans in _extentUtil for when an extent is passed

replacing of divExtent on zoom with forward calc'd extent. patchDivExtent

